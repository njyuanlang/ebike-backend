module.exports = function(Upload) {
  
  // Upload.beforeRemote("*", function (ctx, unused, next) {
  //   console.log(ctx.req.headers, ctx.req.params)
  //
  //   if(ctx.req.body.search(reg) != -1) {
  //     console.log('Found base64')
  //     ctx.req.body = ctx.req.body.replace(reg, "")
  //     ctx.req.body = ctx.req.body.replace(/ /g, '+')
  //     ctx.req.body = new Buffer(ctx.req.body, 'base64')
  //   }
  //   next()
  // })
  
};
